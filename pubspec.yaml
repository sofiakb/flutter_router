name: sofiakb_router
description: The packages allows to manage routes.
maintainer: Sofiane Akbly
version: 2.0.0-dev.2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

scripts:
  release: ./scripts/release.sh